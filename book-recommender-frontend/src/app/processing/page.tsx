import PageLayout from "@/components/ui/PageLayout/PageLayout";

/**
 * Placeholder carried over from the Vite app: the questionnaire currently
 * navigates straight to the results, so nothing links here yet.
 */
export default function ProcessingPage() {
  return (
    <PageLayout>
      <h1>Processing</h1>
    </PageLayout>
  );
}
