# Where the FastAPI backend lives, as seen from the Next server.
# Read at request time, so changing it needs a restart, never a rebuild.
# Local development: http://127.0.0.1:8000
# Docker compose:    http://backend:8000
BACKEND_URL=http://127.0.0.1:8000
